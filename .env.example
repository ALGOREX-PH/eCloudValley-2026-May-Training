# Copy this file to `.env` and fill in your key.
# The speaker will share a workshop key with you before Day 1.

OPENAI_API_KEY=sk-replace-me

# Optional: override the default model used in labs
# OPENAI_MODEL_ID=gpt-4o-mini

# Optional: override the default embedding model used in RAG labs
# OPENAI_EMBEDDING_MODEL=text-embedding-3-small
